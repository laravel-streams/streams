{
    "name": "streams/streams",
    "description": "",
    "type": "project",
    "keywords": [
        "laravel",
        "control panel",
        "cmf"
    ],
    "license": "MIT",
    "authors": [
        {
            "name": "Ryan Thompson",
            "email": "ryan@pyrocms.com"
        }
    ],
    "require": {
        "streams/core": "2.0.x-dev",
        "streams/api": "1.0.x-dev",
        "streams/ui": "1.0.x-dev"
    },
    "require-dev": {
        "barryvdh/laravel-debugbar": "^3.3",
        "streams/dev": "1.0.x-dev",
        "fzaninotto/faker": "^1.9",
        "facade/ignition": "^2.0",
        "mockery/mockery": "^1.3",
        "laravel/tinker": "^2.2",
        "phpunit/phpunit": "^9.0"
    },
    "repositories": [
        {
            "type": "composer",
            "url": "https://packages.pyrocms.com"
        }
    ],
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "": "tests/"
        }
    },
    "extra": {
        "dont-discover": []
    },
    "prefer-stable": false,
    "minimum-stability": "dev",
    "config": {
        "bin-dir": "bin",
        "preferred-install": "dist",
        "optimize-autoloader": true,
        "sort-packages": true
    },
    "scripts": {
        "pre-autoload-dump":[
            "rm -Rf bootstrap/cache/*.php"
        ],
        "post-autoload-dump": [
            "Illuminate\\Foundation\\ComposerScripts::postAutoloadDump",
            "@php artisan package:discover --ansi",
            "@php artisan clear --ansi",
            "@php artisan vendor:publish --ansi --tag=public",
            "@php artisan queue:restart --ansi"
        ],
        "post-root-package-install": [
            "@php -r \"file_exists('.env') || copy('.env.example', '.env');\""
        ],
        "post-create-project-cmd": [
            "@php artisan key:generate --ansi"
        ]
    }
}
