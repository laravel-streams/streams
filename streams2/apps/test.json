{
    "match": "*.streams.local*",
    "config": {
        "app.name": "Dah!"
    },
    "streams": {
        "pages": {
            "source.path": "streams/data/pages/{request.parsed.domain.0}"
        }
    }
}
